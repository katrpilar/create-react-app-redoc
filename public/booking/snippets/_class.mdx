### Custom CSS Classes 
This parameter is used to specify a custom class for each tiles step in case you want to style two tiles steps differently. In the custom theme you will need to define a class named `{className}CustomClass` where {`className`} needs to be replaced with the value added to this parameter. For the example below you will have `differentTileCustomClass`