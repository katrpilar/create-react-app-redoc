### Feather Icons
This parameter is used to specify an icon when displaying the selected value in  the appointment summary. If an icon is provided,  then, we don’t show the label. For this we are using the [Feather icon system](https://feathericons.com/). You need to specify the icon name and we will automatically show that icon.